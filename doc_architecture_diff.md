# 总体架构升级与业务优化说明文档

本说明文档基于当前工作区与上一次 push 版本的完整 `git diff`，总结本次微服务架构调整、公共能力下沉以及核心业务链路优化内容，重点覆盖实际代码中已落地的改造。

---

### 1. 公共内核下沉与微服务分层规范化
* 针对各微服务重复维护通用配置、异常、事件、认证模型和工具类，完成 `shared` 包向 `common` 公共模块的归并，并将领域模型、应用服务、基础设施等职责重新整理，统一 Feign、Redis、MQ、鉴权、异常处理及文件能力的依赖入口；各业务服务保留独立的 API、Application、Domain、Infrastructure 分层，降低模块耦合并提升后续迭代的一致性。

### 2. 配置中心与服务基础设施治理
* 针对多服务本地配置重复、环境切换成本高及网关路由维护不集中等问题，引入 Nacos 配置加载和动态路由能力，统一数据库、Redis、消息队列、对象存储等基础配置；同时补充 Feign OkHttp 客户端和公共基础设施配置，减少服务内重复配置与资源文件冗余。

### 3. 数据库标识与表结构升级
* 统一业务表使用数据库自增主键，并补充 `public_id` 雪花 ID 作为对外业务标识，兼顾数据库写入效率与接口数据安全；同步完善 MyBatis-Plus 元数据填充、公共持久化对象及二级索引结构，减少内部自增 ID 直接暴露和跨服务数据结构不一致问题。

### 4. 高频互动数据缓存化与批量落库
* 针对帖子、评论点赞/收藏、浏览量和回复数等高频操作直接写库造成行锁竞争与数据库瞬时压力的问题，采用 Redis Hash、Set/ZSet 记录热点计数及用户互动状态，通过 Pipeline 批量读取和缓存预热降低网络往返；使用变更集合与定时任务合并增量，按批次同步 MySQL，并在同步过程中进行差集计算，避免逐条更新和 N+1 查询。

### 5. 热度计算与排行榜缓存优化
* 针对热度分数频繁变更导致 Redis 和数据库重复写入的问题，引入本地增量缓冲、buffer swap 和 Redis Pipeline 批量刷新机制，将创作者互动增量写入周期性周榜 ZSet；结合周期过期、TopN 裁剪及事件监听更新帖子/创作者榜单，在保证榜单实时性的同时控制缓存规模和持久化压力。

### 6. 帖子 Feed 与评论游标分页
* 针对传统页码分页在持续滑屏时出现重复/遗漏以及深分页性能下降的问题，帖子和评论查询引入游标分页及缓存 ZSet 查询；热门和最新列表通过稳定排序游标获取 ID，再采用批量查询、缓存优先和内存装配补齐作者、标签、图片、互动状态等信息，避免复杂大表 Join 及逐条远程查询，提升大数据量 Feed 和评论列表的查询稳定性。

### 7. 订单超时链路的 Redis 与 MySQL 双重兜底
* 针对 Redis 不可用时订单无法进入延迟处理队列的问题，订单创建时将 `timeout_time` 固化为创建时间加业务 TTL，并在数据库事务提交后的 `afterCommit` 阶段注册 Redis ZSet 超时记录，避免缓存异常影响订单落库；定时任务在 Redis 异常或恢复窗口内使用带状态和超时时间条件的批量 SQL 扫描待支付订单，通过领域方法完成状态迁移和批量更新，并补充 `(status, timeout_time, id)` 联合索引，保证订单超时处理可恢复、可幂等。

### 8. 优惠券高并发库存安全
* 针对活动领取场景的库存超卖、重复领取及数据库直接扣减性能瓶颈，将资格校验、用户领取限制和库存扣减封装至 Redis Lua 脚本中，利用 Redis 原子执行保证并发安全；通过缓存预热、分布式锁保护关键重建流程，并由定时任务批量将缓存库存结果同步回数据库，降低活动高峰期数据库承压。

### 9. 订单支付与领域事件异步解耦
* 针对订单、支付、会员权益和消息通知之间强耦合导致事务链路变长的问题，统一支付成功/失败、订单支付、帖子互动、用户注册等领域事件模型，结合 RabbitMQ 监听器异步驱动跨服务处理，并通过状态校验、幂等标记和批量查询降低重复消费及链路失败带来的数据不一致风险。

### 10. 安全能力与前端交互协同优化
* 针对用户输入、文件访问和接口资源暴露等风险，统一敏感词/XSS 处理、鉴权拦截、IP 地域库和文件能力配置，并清理各服务重复资源；同步调整首页 Feed、支付页面、评论展示及消息页面的接口适配和交互布局，使前后端数据结构升级与核心业务链路保持一致。
