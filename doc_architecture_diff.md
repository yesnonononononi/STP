# 逻辑架构与本地代码差异说明文档

本说明文档通过对比远程仓库上一次 push（提交 `8ea29a1`）的描述与当前本地代码逻辑架构，总结了以下优化与新增内容：

---

### 1. 表情符号（Emoji）解析与排版优化
* 为了解决前后端表情存储格式不一致（中括号匹配差异），以及在帖子列表、帖子详情、评论区、私信聊天等多模块下表情图片包装零散、对齐不佳的问题，采用了“双向兼容正则匹配 + 自适应 HTML 标签格式化与居中对齐样式”方案，使得多场景下的表情还原高度准确且排版等比对齐。

### 2. 帖子标签（Tag）展示与收折异常修复
* 为了解决标签单独占用布局节点影响排版紧凑性，以及标签转化为 HTML 超链接后源码长度干扰字数计算导致短帖子异常显示“展开”按钮的问题，采用了“正文尾部 v-html 内联拼接展示 + 展开判定隔离纯文本”方案，使得标签以淡蓝色超链接自然依附在内容末端，且“展开/收起”按钮在非超长文本下不再误触发。

### 3. 多标签页 WebSocket 通信与连接重用
* 为了解决浏览器多标签页打开时重复握手建立 WebSocket 连接造成服务端开销过高、心跳包泛滥与状态重连不一致的问题，采用了“WebSocket 网络层下沉至 SharedWorker 共享线程网关”方案，使得同源页面共享复用单条物理长连接进行心跳重连，但同时可能在部分不支持 SharedWorker 的低版本浏览器或非 HTTPS 安全域名下导致连接失效（需主线程连接保底支持）。

---

### 4. 私信与即时通讯系统
* & 新增**私信即时通讯与陌生人会话展示功能**，基于**后端领域驱动设计（DDD）架构的消息微服务与前端 SharedWorker 共享网关的事件派发机制**实现。系统集成了消息发送的 XSS 安全过滤、基于会话模型（MessageSession）的双端未读数统计与已读同步状态，利用滚动游标分页机制（Cursor-based Pagination）保障大消息量下的历史数据高性能拉取，并在前端支持自适应左右气泡布局以及通过格式化管道实时渲染内联表情。

### 5. 并发控制与资源争抢防范
* & 新增**分布式环境下的高并发资源争抢防范机制**，基于**Redisson 分布式锁及 AOP 切面协调**实现。系统在涉及高并发冲突的临界区代码（如优惠券兑换、会员等级与时效折算）中引入 Redisson 的 `RLock` 锁，以用户 ID 或业务资源 ID 构造细粒度锁 Key，防止多实例节点部署时产生并发竞争冲突与行级死锁。
